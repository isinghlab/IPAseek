// SLURM executor configuration
// Edit --account to match your HPC allocation
process {
    executor     = 'slurm'
    // TODO: replace 'your_account' with your actual SLURM account name
    clusterOptions = '--account=your_account'
    queue        = 'normal'
}
